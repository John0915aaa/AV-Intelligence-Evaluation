# AV-Intelligence-Evaluation
Traditional ADS evaluation depends on subjective expert scoring or fixed metric weights, which struggle with nonlinear behavior and consistency. Our method replaces expert labeling with an LLM-driven evaluation process, producing stable, human-aligned ground truth for training an objective model.
